{
  "devDependencies": {
    "@types/node": "^20.10.2",
    "@types/svg-parser": "^2.0.6",
    "typescript": "^5.3.2"
  },
  "name": "svg-to-json",
  "version": "0.9.0",
  "description": "Convert svg files in folder itno json file",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "rm -f ./dist/bundle.js && tsc --build && echo '#!/usr/bin/env node' | cat - ./dist/bundle.js > temp && mv  temp ./dist/bundle.js && chmod +x ./dist/bundle.js",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json",
    "svg"
  ],
  "bin": {
    "svgToJson": "./dist/bundle.js"
  },
  "author": "Milan Macháček",
  "license": "ISC",
  "dependencies": {
    "link": "^2.1.0",
    "svg-parser": "^2.0.4",
    "svgo": "^3.0.5"
  }
}
